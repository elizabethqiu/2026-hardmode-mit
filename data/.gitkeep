# Database

`enoki.db` lives here; per `pattern_learn.py` the SQLite database will write here every 5 seconds. Every state tick (FOCUSED, IDLE, DOZING, AWAY) gets logged here with timestamp, hour, day-of-week, and raw sensor values. This is what the RandomForest trains on nightly.

